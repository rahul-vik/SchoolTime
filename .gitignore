node_modules/
dist/
**/__pycache__/
*.pyc
.vite/
SchoolTime-workspace.zip

# Environment and secrets
.env
.env.local
.env.*.local
pem/
*.pem

# Database and runtime artifacts
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal

# Local export/diagnostic output (npm run export:last-run, PDF exports)
Results/

# Agent/session temp scripts (never commit)
scripts/tmp-*.mjs

# Logs and OS files
*.log
.DS_Store
Thumbs.db

# Local UI/debug screenshots (not part of repo assets)
reportUI.png
reportUI2.png
timetable missmatch.png

# IDE/editor — commit shared tasks; ignore other local VS Code files
.idea/
.vscode/*
!.vscode/tasks.json
!.vscode/extensions.json

